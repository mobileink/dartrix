# ~/.dartrix
alias {{prefix}}list="pub global run{{#enable-asserts}} --enable-asserts{{/enable-asserts}} dartrix:list $@"
alias {{prefix}}man="pub global run{{#enable-asserts}} --enable-asserts{{/enable-asserts}} dartrix:man $@"
alias {{prefix}}new="pub global run{{#enable-asserts}} --enable-asserts{{/enable-asserts}} dartrix:new $@"
alias {{prefix}}sanity="pub global{{#enable-asserts}} run --enable-asserts{{/enable-asserts}} dartrix:sanity $@"
alias {{prefix}}update="pub global{{#enable-asserts}} run --enable-asserts{{/enable-asserts}} dartrix:update $@"
alias {{prefix}}version="pub global{{#enable-asserts}} run --enable-asserts{{/enable-asserts}} dartrix:version $@"
